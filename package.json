{
  "name": "online-multiplayer",
  "version": "0.1.0",
  "private": true,
  "description": "Reusable online multiplayer game backbone (Colyseus) + demo games",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run typecheck && npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run test -w server",
    "smoke": "node scripts/smoke-e2e.mjs",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "concurrently": "^9.1.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
